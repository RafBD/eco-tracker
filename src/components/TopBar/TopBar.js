import React from 'react';
import SideBar from '../SideBar/SideBar';
import './TopBar.css';

const TopBar = () => {

    return (
        <div className="topbar">
            <SideBar />
        </div>
    )
}

export default TopBar;